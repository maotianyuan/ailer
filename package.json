{
  "name": "root",
  "version": "0.0.1",
  "private": false,
  "scripts": {
    "publish": "lerna bootstrap && lerna run build && lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
